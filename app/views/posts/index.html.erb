<h1>Описание фильмов</h1>
<% @post.each do |post| %>
    <div class="alert alert-light">
        <h3><%= post.title %></h3>
        <h5><%= post.body %></h5>
        <%= link_to "Читать далее", post_path(post), :class => 'btn btn-outlina-primary' %>
    </div>
<%end%>
